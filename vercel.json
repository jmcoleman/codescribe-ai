{
  "version": 2,
  "buildCommand": "cd server && npm install && npm run migrate && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --prefix server && npm install",
  "cleanUrls": true,
  "git": {
    "deploymentEnabled": false
  },
  "ignoreCommand": "bash -c 'if [ \"$VERCEL_GIT_COMMIT_REF\" = \"gh-pages\" ]; then exit 0; else git diff --quiet HEAD^ HEAD client/ server/ package.json vercel.json && exit 0 || exit 1; fi'",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=63072000; includeSubDomains; preload"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  }
}
